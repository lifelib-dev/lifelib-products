# Runtime requirements for the reference models under <library>/products/<product>/.
#
# The models are serialized with modelx serializer v8, which 0.31.1 cannot read. 0.32 is
# the first release that can, and PyPI ships it now, so a plain `pip install -r` is all
# this needs -- the checkout of github.com/fumitoh/modelx this note used to call for is no
# longer necessary, and CI does not do it either.
modelx>=0.32

pandas>=2.2
numpy>=1.26

# Tests only.
pytest>=8

# Deliberately absent:
#   lifelib  - the models are plain modelx and do not import it.
#   openpyxl - all model inputs are CSV, so no Excel reader is needed.
#   scipy    - no model here needs anything beyond the standard library's math.
